# Security Policy

Please report security issues privately instead of opening a public issue.

The desktop application is local-first. It only scans the Markdown directory
selected by the user. Cloud sync is optional and must use HTTPS, except for
explicit localhost development endpoints.

Never attach a real vault, database, pairing secret, private key, access token,
or `.env` file to an issue.
